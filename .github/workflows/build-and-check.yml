name: Build and Check Browser Extension

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  pull-requests: write

jobs:
  build-and-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          fetch-depth: 0  # 获取完整的 git 历史和所有 tags

      - name: Setup Node.js
        uses: actions/setup-node@1d0ff469b7ec7b3cb9d8673fde0c81c44821de2a  # v4.2.0
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Dependency audit
        run: npm audit --production || true
        continue-on-error: true

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: Lint check
        run: |
          if [ -f "package.json" ] && grep -q "eslint" package.json; then
            npm run lint
          else
            echo "ℹ️ ESLint not configured, skipping lint check"
          fi

      - name: Format check
        run: |
          if [ -f "package.json" ] && grep -q "prettier" package.json; then
            npm run format:check
          else
            echo "ℹ️ Prettier not configured, skipping format check"
          fi

      - name: Run unit tests
        run: |
          if [ -f "package.json" ] && grep -q "vitest" package.json; then
            npm run test:coverage
          else
            echo "ℹ️ Vitest not configured, skipping unit tests"
          fi

      - name: Upload coverage reports
        if: always()
        uses: codecov/codecov-action@9cc74bf7e13a810454a28846494ddbc3795eb693  # v3.1.6
        with:
          files: ./coverage/coverage-final.json
          flags: unittests
          fail_ci_if_error: false

      - name: Build extension
        run: npm run build

      - name: Verify build success
        run: |
          if [ ! -d "build" ]; then
            echo "✗ Build directory not found"
            exit 1
          fi
          if [ ! -d "build/chrome-mv3-prod" ]; then
            echo "✗ Production build directory not found"
            exit 1
          fi

      - name: Get project version
        id: get_version
        run: |
          VERSION=$(jq -r '.version' package.json)
          PROJECT_NAME=$(jq -r '.name' package.json)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "project_name=$PROJECT_NAME" >> $GITHUB_OUTPUT
          echo "build_dir=${PROJECT_NAME}-${VERSION}" >> $GITHUB_OUTPUT
          echo "zip_file=${PROJECT_NAME}-${VERSION}.zip" >> $GITHUB_OUTPUT

      - name: Package extension
        run: npm run package

      - name: Rename build artifacts
        run: |
          VERSION=${{ steps.get_version.outputs.version }}
          PROJECT_NAME=${{ steps.get_version.outputs.project_name }}
          BUILD_DIR=${{ steps.get_version.outputs.build_dir }}
          ZIP_FILE=${{ steps.get_version.outputs.zip_file }}
          
          # Rename build directory
          if [ -d "build/chrome-mv3-prod" ]; then
            mv build/chrome-mv3-prod build/$BUILD_DIR
            echo "✓ Renamed build directory to build/$BUILD_DIR"
          fi
          
          # Rename zip file
          if [ -f "build/chrome-mv3-prod.zip" ]; then
            mv build/chrome-mv3-prod.zip build/$ZIP_FILE
            echo "✓ Renamed zip file to build/$ZIP_FILE"
          fi

      - name: Verify zip file creation
        run: |
          ZIP_FILE=$(find build -name "*.zip" | head -1)
          if [ -f "$ZIP_FILE" ]; then
            echo "✓ Zip file created: $ZIP_FILE"
            ls -la "$ZIP_FILE"
            # Check zip file size
            ZIP_SIZE=$(stat -c "%s" "$ZIP_FILE")
            echo "✓ Zip file size: $ZIP_SIZE bytes"
            if [ "$ZIP_SIZE" -lt 1000 ]; then
              echo "⚠️ Zip file is very small, may be incomplete"
            fi
          else
            echo "✗ Zip file not found"
            exit 1
          fi

      - name: Verify build artifacts
        run: |
          echo "📁 Build artifacts:"
          ls -la build/
          BUILD_DIR=$(find build -type d -name "*-*" | head -1)
          if [ -d "$BUILD_DIR" ]; then
            echo "📁 Production build:"
            ls -la "$BUILD_DIR"
            # Check for required files
            REQUIRED_FILES=("manifest.json" "popup.html")
            for file in "${REQUIRED_FILES[@]}"; do
              if [ -f "$BUILD_DIR/$file" ]; then
                echo "✓ $file exists"
              else
                echo "✗ $file missing"
                exit 1
              fi
            done
            # Check for background script
            if [ -f "$BUILD_DIR/static/background/index.js" ]; then
              echo "✓ background script exists at static/background/index.js"
            else
              echo "ℹ️ background script not found"
            fi
          else
            echo "✗ Build directory not found"
            exit 1
          fi

      - name: Upload build artifacts
        uses: actions/upload-artifact@65c4c4a1ddee5b72f698fdd19549f0f0fb45cf08  # v4.6.0
        with:
          name: extension-build-${{ steps.get_version.outputs.version }}
          path: |
            build/
          retention-days: 7

      - name: Install Playwright browsers
        run: |
          if [ -f "package.json" ] && grep -q "@playwright/test" package.json; then
            npx playwright install --with-deps
          else
            echo "ℹ️ Playwright not configured, skipping E2E tests"
          fi

      - name: Run E2E tests
        run: |
          if [ -f "package.json" ] && grep -q "@playwright/test" package.json; then
            npm run test:e2e
          else
            echo "ℹ️ Playwright not configured, skipping E2E tests"
          fi

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@65c4c4a1ddee5b72f698fdd19549f0f0fb45cf08  # v4.6.0
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Check manifest validity
        run: |
          if [ -f "package.json" ]; then
            echo "✓ Checking manifest configuration in package.json"
            cat package.json | grep -A 20 "manifest"
          else
            echo "✗ package.json not found"
            exit 1
          fi

      - name: Check manifest.json
        run: |
          BUILD_DIR=$(find build -type d -name "*-*" | head -1)
          if [ -f "$BUILD_DIR/manifest.json" ]; then
            echo "✓ manifest.json exists"
            cat $BUILD_DIR/manifest.json
            # Validate manifest.json format
            if command -v jq &> /dev/null; then
              jq '.' $BUILD_DIR/manifest.json
              echo "✓ manifest.json is valid JSON"
            else
              echo "ℹ️ jq not available, skipping JSON validation"
            fi
          else
            echo "✗ manifest.json not found"
            exit 1
          fi

      - name: Security check
        run: |
          echo "🔒 Running security checks..."
          # Check for common security issues
          if grep -r "eval(" --include="*.ts" --include="*.tsx" --include="*.js" .; then
            echo "⚠️  Found eval() usage, potential security risk"
          else
            echo "✓ No eval() usage found"
          fi
          if grep -r "innerHTML" --include="*.ts" --include="*.tsx" --include="*.js" .; then
            echo "⚠️  Found innerHTML usage, potential XSS risk"
          else
            echo "✓ No innerHTML usage found"
          fi

      - name: Generate Release Notes
        if: github.event_name == 'workflow_dispatch'
        id: generate_notes
        run: |
          # 显示所有 tags
          echo "📋 Available tags:"
          git tag -l | sort -V
          
          # 获取上一个 tag
          PREVIOUS_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          echo "🏷️ Previous tag: $PREVIOUS_TAG"
          
          # 获取当前版本
          CURRENT_VERSION="${{ steps.get_version.outputs.version }}"
          echo "📌 Current version: v$CURRENT_VERSION"
          
          if [ -z "$PREVIOUS_TAG" ]; then
            # 如果没有上一个 tag，生成所有历史记录
            echo "ℹ️ No previous tag found, generating full changelog"
            NOTES=$(npx git-cliff --strip header)
          else
            # 生成从上一个 tag 到现在的变更
            echo "ℹ️ Generating changelog from $PREVIOUS_TAG to HEAD"
            NOTES=$(npx git-cliff "$PREVIOUS_TAG..HEAD" --strip header)
          fi
          
          # 显示生成的内容（用于调试）
          echo "📝 Generated notes:"
          echo "$NOTES"
          
          # 如果没有生成内容，使用默认内容
          if [ -z "$NOTES" ] || [ "$NOTES" = "## [Unreleased]" ]; then
            echo "⚠️ No notes generated, using default"
            NOTES=$(printf "## What's Changed\n\n- 版本更新至 %s\n- 详见提交历史" "$CURRENT_VERSION")
          fi
          
          # 设置多行输出
          echo "notes<<EOF" >> $GITHUB_OUTPUT
          echo "$NOTES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          echo "✓ Release notes generated"

      - name: Create Release
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@c95fe1489396fe8a9eb87c0abf8aa5b2ef267fda  # v2.2.1
        with:
          tag_name: v${{ steps.get_version.outputs.version }}
          name: Release v${{ steps.get_version.outputs.version }}
          body: ${{ steps.generate_notes.outputs.notes }}
          files: |
            build/*.zip
          draft: false
          prerelease: false

      - name: Summary
        run: |
          echo "## Build Summary"
          echo "✅ All checks passed"
          echo "✅ Build completed successfully"
          echo "✅ TypeScript check passed"
          echo "✅ Dependency audit completed"
          echo "✅ ESLint check passed"
          echo "✅ Prettier check passed"
          
          if [ -f "package.json" ] && grep -q "vitest" package.json; then
            echo "✅ Unit tests passed"
          else
            echo "ℹ️ Vitest not configured, skipping unit tests"
          fi
          
          if [ -f "package.json" ] && grep -q "@playwright/test" package.json; then
            echo "✅ E2E tests passed"
          else
            echo "ℹ️ Playwright not configured, skipping E2E tests"
          fi
          
          echo "✅ Extension packaged"
          echo "✅ Build artifacts uploaded"
          
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "✅ Release created"
            echo "✅ Ready for deployment"
          else
            echo "ℹ️ Release step skipped (manual trigger only)"
          fi
